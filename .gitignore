node_modules/
dist/
.vite/
*.local
.DS_Store

# Evidence bundles ARE versioned on purpose: harness verdicts must bind to a
# commit SHA. Only the raw video blobs Playwright leaves behind are excluded.
evidence/**/video/*.tmp
test-results/
playwright-report/

# Vercel CLI: the local project link, and the env file it writes on `vercel link`.
.vercel
.env*
